import React, { Component } from "react";

class LoginPractice extends Component {
  render() {
    return (
      <div>
        Login Practice
        <ul>
          <li>
            <div>yarn add redux react-redux redux-thunk</div>
            <div>yarn add redux-logger redux-devtools-extension --dev</div>
            <div>touch filname (create new file)</div>
            <div>npm install express --save</div>
            <div>
              when trying to run node filename u will found unable to start the
              server due to import statement in node
            </div>
            <div>
              npm install --save-dev babel-cli babel-preset-env (using this one
              to overcome the problem)
            </div>
            <div>npm install nodemon --save-dev</div>
            <div>yarn add react-router-dom</div>
            <div>in order to display json format need install body-parser</div>
            <div>
              in order to check whether those email and password filled in use
              validator package to do for you
              <div>npm install validator --save</div>
            </div>
            <div>auto generate id npm install shortid</div>
          </li>
        </ul>
      </div>
    );
  }
}

export default LoginPractice;
